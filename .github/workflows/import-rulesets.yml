name: Import Rulesets
on:
  push:
    paths:
      - .github/rulesets/**
  workflow_dispatch:

jobs:
  import:
    uses: SgtSwagrid/github-rules/.github/workflows/import-rulesets.yml@main
    secrets: inherit
